# 参与贡献

感谢关注这个项目。这是一个按「朋友间小圈子使用」打磨到 v1.0 的工具，
欢迎 issue、fork 与 PR。

## 动手前必读

1. **`docs/CONVENTIONS.md`** —— 按任务查：这次改动属于哪类、必读哪节、
   跑什么命令、这个领域历史上踩过什么坑。这里的经验都是实打实踩出来的，
   先查表能省掉大部分返工。
2. **`docs/ARCHITECTURE.md` §4** —— 某条约束「为什么这么定」。
   里面有些结论反直觉（例如采集音频必须显式关掉 AEC/NS/AGC），改动前先看原委。
3. **`docs/ROADMAP.md`** —— 当前明确不做的（M7~M10 标注了理由）。
   想做其中某项？先开 issue 讨论。

## 提 PR 的规矩

- **验收脚本跟着功能走。** 这个项目的门禁全部可无人值守执行，
  新功能必须带新的自动化验收；修 bug 请附一条「改动前会红」的断言。
- 改了信令 / 采集 / 协商 / 打包配置，对应门禁必跑
  （`CONVENTIONS.md` 里每类任务都标了「必跑」清单）。
- 断言写「实际值 == 按协议算出的期望值」，不写「看起来对了」。
- 关键判据要有对照组，关键断言要反向验证过确实会红。
- 提交信息说清「行为上改了什么」，而不是「改了哪些文件」。

## 环境问题

Windows 10 / 11、Node ≥ 20。开发机特定的环境坑（npm 被拦截、electron-builder
缓存与文件锁等）见 `docs/BUILD-NOTES.md`——不是每个人都会遇到，遇到了别慌。

## 方向讨论

M7（网络状态面板）/ M8（TURN 中继与鉴权）/ M9（稳定性）/ M10（性能基线）
是当前明确的空白区，欢迎在此之前开 issue 提议其它方向。
涉及安全模型（鉴权 / 限流 / 密钥管理）的改动，请务必先开 issue 达成一致再动手。
